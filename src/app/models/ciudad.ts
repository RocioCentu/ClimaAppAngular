//import { SecureContextOptions } from 'tls';

export class Ciudad {
    id: string;
    name: string;
    temp: string;
    humidity: string;
    wheater: string;
    province: string;
    pressure: string;
    visibility: string;
    wind_speed: string;
    temp_min: string;
    temp_max: string;
    date: string;
}